services:
  android-builder:
    build:
      context: .
      dockerfile: Dockerfile.android
    volumes:
      # Mounts a folder on your Windows machine to receive the output APK
      - ./build-output:/app/mobile/android/app/build/outputs/apk